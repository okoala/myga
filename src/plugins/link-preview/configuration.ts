import { pluginId, pluginName, pluginDesc } from './constants';

const LinkPreviewConfiuration = {
  schema: {
    id: pluginId,
    title: pluginName,
    description: pluginDesc,
  },
  defaultValue: {},
};

export default LinkPreviewConfiuration;
